# Copy this file to <project>.conf and adjust it for that project.

PROJECT_PATH="~/Projects/example"
DEV_SESSION="dev-example"

DEV_BOOTSTRAP_ONCE=(
  "mise install"
)

DEV_BOOTSTRAP=(
  "git status --short"
)
